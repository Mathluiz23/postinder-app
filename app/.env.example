# URL do backend. Use o IP da sua rede local (não "localhost"), para
# funcionar também em celular físico via Expo Go. Confirme a porta em
# backend/.env.example (padrão: 4000).
EXPO_PUBLIC_API_URL=http://SEU_IP_LOCAL:4000
